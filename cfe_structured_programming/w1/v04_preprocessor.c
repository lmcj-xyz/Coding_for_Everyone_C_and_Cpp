/* The proprocessor
 * 24 Oct
 * LM
 */
#include <stdio.h>

/*
 * "#" happens at column 1 and indicates a preprocessor instruction
 */
int main (void)
{
	int const length = 4;
	int v[length] = {1, 2, 3, 4};
	return 0;
}
